services:
  - type: web
    name: medical-prediagnosis-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python model/train_model.py
    startCommand: gunicorn app:app --workers 2 --threads 4 --timeout 120
    healthCheckPath: /health
    autoDeploy: true
